import ClosetClient from "./ClosetClient";

export const metadata = {
  description: "Your saved styles, recent views, and generated fit guidance.",
};

export default function ClosetPage() {
  return <ClosetClient />;
}

